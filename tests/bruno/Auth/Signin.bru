meta {
  name: Signin
  type: http
  seq: 2
}

post {
  url: http://localhost:6081/auth/signin
  body: json
  auth: none
}

body:json {
  {
    "email": "jean.dupont@example.com",
    "password": "password123"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("token", res.body.access_token);
  }
}
